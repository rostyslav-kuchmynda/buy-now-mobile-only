export type FeatureTypes = {
  illustration: string;
  bgColor: string;
  title: string;
  subTitle: string;
  bullets: Array<BulletTypes>;
};

export type BulletTypes = {
  icon: string;
  desc: string;
};
